# Ejemplo de variables de entorno
# Copia este archivo a .env y configura tus valores reales

# Base de datos
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password
DB_NAME=asistencias_db
DB_DIALECT=mysql

# JWT
JWT_SECRET=tu_secreto_super_seguro_aqui_cambiar

# Puerto (Railway/Render lo asigna automáticamente)
PORT=3000

# Entorno
NODE_ENV=production
